# PDF 原理图人工核对清单（示例）

> 本文件是 `outputs/pdf_review.md` 的格式示例（数据来自正点原子战舰板
> STM32F103ZET6 原理图 PDF 的实际解析结果）。PDF 输入时 S1 自动生成。

- 源文件：`schematic/WarShip STM32F1_V3.4_SCH.pdf`
- 生成时间：2026-09-23T08:30:50
- 整体置信度：**0.808**
- 元件数：233，网络数：290，引脚连接数：660

> PDF 提取不保证 100% 精确（矢量图形 + 文字推断的技术本质决定）。
> 置信度分级：≥ 0.95 高可信直接使用；0.7 ~ 0.95 中可信（S4 重点核对）；
> < 0.7 低可信（本清单，必须人工核对）。

## 一、需要人工核对的连接（置信度 < 0.7，共 155 条）

| 引脚 | 推断网络 | 推断目标 | 置信度 | 建议 |
|---|---|---|---|---|
| U16.3（INA-） | Aref | U16.5、R50.1、R57.2 | 0.5 | 请人工确认 U16.3（INA-）是否连接到网络 Aref |
| U19.29（S1） | VS_SCK | U2.42 | 0.5 | 请人工确认 U19.29（S1）是否连接到网络 VS_SCK |
| D2.24（SD13） | FSMC_D15 | X2.24、X1.24、U1.37 | 0.5 | 请人工确认 D2.24（SD13）是否连接到网络 FSMC_D15 |
| U17.3（IN-） | COR58 | R58.2 | 0.5 | 请人工确认 U17.3（IN-）是否连接到网络 COR58 |

（实际文件按置信度升序列出全部低置信度连接，每条含判定方法与说明。）

## 二、建议在 S4 阶段核对的连接（置信度 0.7 ~ 0.95，共 482 条）

| 引脚 | 推断网络 | 推断目标 | 置信度 | 建议 |
|---|---|---|---|---|
| C43.1 | GND | （其他端点略） | 0.9 | 建议在 S4 电路核查阶段重点核对 C43.1 与 GND 的连接 |
| U12.3 | VCC5 | （其他端点略） | 0.9 | 建议在 S4 电路核查阶段重点核对 U12.3 与 VCC5 的连接 |

（实际文件列出全部中置信度连接。）

## 三、未识别到引脚的元件（可能为无框符号，请人工确认）

| 页 | 元件 | 说明 |
|---|---|---|
| 第2页 | LED0 | 无框符号可能未标引脚号 |
| 第3页 | D210 | 无框符号可能未标引脚号 |

---

### 工程师使用说明

1. 优先核对**第一节**（低置信度，多为引脚邻近推断与断线场景）；
2. **第三节**元件如确属设计一部分，需人工补充引脚连接（LED/二极管等）；
3. MCU 电源脚（VDD/VSS）在 PDF 中无引脚号文字，不由本清单覆盖——
   S3 阶段从数据手册补全；
4. 核对完成后，可直接修正 `outputs/circuit_netlist.json`，或在 S4 阶段
   以本清单为输入做电路核查。
